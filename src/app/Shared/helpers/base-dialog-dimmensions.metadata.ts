import { MatDialogConfig } from '@angular/material/dialog';

export const BaseDialogDimensions: MatDialogConfig = {
	hasBackdrop: true,
	minHeight: 200,
	minWidth: 500,
	maxWidth: 1500,
	role: 'alertdialog',
};
